export const playGuitar = () => {
    return 'Playing guitar'
}

export const playBass = () => {
    return 'Playing bass'
}

// export { playGuitar, playBass } // this will work but we must remove the export on every function